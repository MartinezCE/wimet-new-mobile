import { useMemo } from 'react';
import { ReservationType } from '../../../services/api';

export const useAddReservation = (
    type: ReservationType = ReservationType['SHARED']
) => {
    return {};
};
